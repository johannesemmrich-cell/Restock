import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            Text("SmartCart")
                .navigationTitle("SmartCart")
        }
    }
}

#Preview {
    ContentView()
}
