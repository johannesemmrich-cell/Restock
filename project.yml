name: Restock
options:
  bundleIdPrefix: com.johannesemmrich
  deploymentTarget:
    iOS: "18.0"

targets:
  Restock:
    type: application
    platform: iOS
    deploymentTarget: "18.0"
    sources:
      - path: SmartCart
    settings:
      base:
        SWIFT_VERSION: 5.9
        PRODUCT_BUNDLE_IDENTIFIER: com.johannesemmrich.Restock
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        TARGETED_DEVICE_FAMILY: "1,2"
        GENERATE_INFOPLIST_FILE: YES
        CURRENT_PROJECT_VERSION: 1
        MARKETING_VERSION: 1.0
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_NSCameraUsageDescription: "SmartCart needs camera access to scan recipe photos."
        INFOPLIST_KEY_NSPhotoLibraryUsageDescription: "SmartCart needs photo library access to import recipe photos."
        INFOPLIST_KEY_UILaunchScreen_BackgroundColor: "LaunchBackground"
